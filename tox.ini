[tox]
env_list =
    py{37,38,39,310,311}
minversion = 4.2.8

[testenv]
description = run the tests with pytest
package = wheel
wheel_build_env = .pkg
deps =
    pytest>=6
extras = tests
commands =
    pytest {tty:--color=yes} {posargs}
